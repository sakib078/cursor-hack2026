# Optional. Leave unset in dev — requests go through the Vite /api proxy.
# Set only if you host the backend elsewhere (e.g. https://api.example.com).
# VITE_API_BASE=

# Where the dev proxy forwards /api (defaults to http://localhost:8000).
# VITE_API_TARGET=http://localhost:8000
